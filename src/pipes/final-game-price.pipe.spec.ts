import { FinalGamePricePipe } from './final-game-price.pipe';

describe('FinalGamePricePipe', () => {
  it('create an instance', () => {
    const pipe = new FinalGamePricePipe();
    expect(pipe).toBeTruthy();
  });
});
